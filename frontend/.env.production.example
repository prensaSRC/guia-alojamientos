# ===== Frontend (producción) — referencia =====
# VITE_API_URL se resuelve en tiempo de build:
#   - En Netlify: Site settings → Environment variables (se setea VITE_API_URL).
#   - Local: copiá este archivo a frontend/.env.production con el valor real
#     (este archivo .example es solo documentación).
VITE_API_URL=https://TU-BACKEND.up.railway.app/api